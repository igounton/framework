# {{PROJECT_NAME}}

> Scaffolded with AIDD. Architecture skeleton only - business logic to follow.

## Stack

- Front-end: {{FRONTEND_FRAMEWORK}}
- Back-end: {{BACKEND_FRAMEWORK}}
- Tests: {{TEST_FRAMEWORK}}
- Database: {{DATABASE}}

Each technical building block (data, plus any of email, auth, storage, jobs, CRON, payments, logging the app uses) wired as a swappable abstraction - see [`INSTALL.md`](INSTALL.md).

## Getting started

Install, configure, run, test instructions: [`INSTALL.md`](INSTALL.md).

## Project docs

- [`INSTALL.md`](INSTALL.md) - technologies, why chosen, how to install / run / test.

## Status

Routes wired with stub handlers (no business logic). Start your first feature in `{{SRC_DIR}}`.
