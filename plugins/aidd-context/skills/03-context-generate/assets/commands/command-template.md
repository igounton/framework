---
description: <one sentence; what the command does and when to use it. Include trigger phrases AND a "Do NOT use" clause.>
argument-hint: <e.g. [issue-number] or [filename] [format]>
model: <inherit | opus | sonnet | haiku>
allowed-tools: <space-separated; e.g. Read Grep Bash(git status *)>
disable-model-invocation: <true | false; default false>
---

# <Command title>

<One-line summary of the command's single objective.>

## Steps

1. <First imperative step. Reference `$ARGUMENTS`, `$0`, `$1`, etc. as needed.>
2. <Next step.>
3. <Final step or output expectation.>

## Notes

- <Optional: shell-injection example via `` !`<read-only command>` ``.>
- <Optional: cross-reference to a skill via `@<path>` when this command delegates a larger flow.>
