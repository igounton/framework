{
  "PreToolUse": [
    {
      "matcher": "{{matcher}}",
      "hooks": [
        {
          "type": "command",
          "command": "{{command}}",
          "timeout": 30
        }
      ]
    }
  ]
}
