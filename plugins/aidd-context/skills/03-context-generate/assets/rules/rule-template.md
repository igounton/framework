---
description: "{{description}}"
paths:
  - "{{glob}}"
---

<!-- AI INSTRUCTIONS ONLY -- follow, do not output.
- description: ONE sentence (what the rule governs + when it applies). Drives rule selection in aidd-dev:01-plan. Precise, not exhaustive.
- paths: optional glob array scoped to real directories (look at the codebase). OMIT the whole key for a project-wide rule. It is no longer an IDE auto-attach trigger - it is a selection hint for the plan.
- Body: bullets only, no prose. One ultra-short (3-7 words) rule per bullet.
- Groups (## headings) and code examples are OPTIONAL - add only when the rule has several logical groups, or an example is truly needed.
-->

# {{rule_title}}

- {{rule_bullet}}
